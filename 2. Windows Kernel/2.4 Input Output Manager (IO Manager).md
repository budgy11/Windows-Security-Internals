- Provides access to IO via device drivers (They are all filesystem drivers in disguise)
- NtLoadDriver syscall or Plug and Play (Pnp) Manager
- Drivers create Device objects via IoCreate Device API
![[Listing Device Objects via OMNS.png]]
- Use File object syscalls to interact with IO Manager
![[Interacting with IO Manager.png]]
- `Get-NtKernelModule` lists drivers loaded in the kernel
- The Ancillary Function Driver (AFD) is an IO manager driver fro networking
	- Uses WinSock to handle access