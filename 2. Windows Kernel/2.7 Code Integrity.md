- Default windows EXEs are all signed with Authenticode
	- Embedded signature in EXE or collected in a catalog file
```powershell
Get-AuthenticodeSignature "$env:WinDir\system32\notepad.exe" | Format-List
```

- Checks if a kernel driver can load based on if the signature is from a Microsoft issued key