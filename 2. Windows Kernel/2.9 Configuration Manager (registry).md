- Like a filesystem where keys are folders and values are files
- Requires registry-specific syscalls when accessing via OMNS
Listing Registry root using NtObject
- `NtKey:\` works too
![[List registry.png]]
A hive is a hierarchy of keys underneath a single root key
```powershell
Get-NtKey

New-NtKey

Remove-NtKey

Get-NtKeyValue

Set-NtKeyValue

Remove-NtKeyValue
```
![[Open Key Object.png]]